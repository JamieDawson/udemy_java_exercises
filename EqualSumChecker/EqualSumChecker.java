public class EqualSumChecker {
     public static boolean hasEqualSum(int one, int two, int three){
        if (one + two == three)
            return (true);
        else
            return (false);
    }

}

/*
 if the first and second int equal the third int, return true. 
 Else, return false.
 */
